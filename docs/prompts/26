I need to be able to display information about tier files. 
I.e. display how much data there is in the file and what time range it covers.

---

write a standalone python script that will also be capable of doing just that

---

I would like to also be able to print latest record from the tier 
in a human readable form (this could be beutified json)

---

ok thank you, now I see the room for optimization
1. CPU for system and self, as well as Net Mbps, Net PPS values are can be rounded to 2 decimal points. Of course we want to round these values in the tier data while we collect them.
2. We can also ignore loop and tmpfs devices and don't store them in the tier data
